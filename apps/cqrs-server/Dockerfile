# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @pooky/cqrs-server`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t pooky-cqrs-server`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system pooky-cqrs-server && \
  adduser --system -G pooky-cqrs-server pooky-cqrs-server

COPY apps/cqrs-server/dist pooky-cqrs-server/
COPY apps/cqrs-server/package.json pooky-cqrs-server/
RUN chown -R pooky-cqrs-server:pooky-cqrs-server .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix pooky-cqrs-server --omit=dev -f install

CMD [ "node", "pooky-cqrs-server" ]
